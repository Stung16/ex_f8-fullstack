<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
      integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
      crossorigin="anonymous"
    />
    <title>Edit Account</title>
  </head>
  <body>
    <div class="py-3 px-5">
      <h1>Đổi mật khẩu</h1>
      <form action="" method="post">
        <div class="mb-3">
          <label for="">Mật khẩu cũ</label>
          <input
            type="password"
            name="passOld"
            value="<%= body?.passOld %>"
            class="form-control"
            placeholder="Mật khẩu ..."
          />
          <p class="text-danger mt-1"><%= req.errors.passOld %></p>
          <%- Erpassold.length ? `<p class="text-danger mt-1">${Erpassold[0]}</p>`: '' %>
        </div>
        <div class="mb-3">
          <label for="">Mật khẩu mới</label>
          <input
            type="password"
            name="passNew"
            value="<%= body?.passNew %>"
            class="form-control"
            placeholder="Mật khẩu ..."
          />
          <p class="text-danger mt-1"><%= req.errors.passNew %></p>
        </div>
        <div class="mb-3">
          <label for="">Nhập lại mật khẩu mới</label>
          <input
            type="password"
            name="retypePass"
            class="form-control"
            value="<%= body?.retypePass %>"
            placeholder="Mật khẩu ..."
          />
          <p class="text-danger mt-1"><%= req.errors.retypePass %></p>
          <%- retypePass.length ? `<p class="text-danger mt-1">${retypePass[0]}</p>`: '' %>
        </div>
        <button type="submit" class="btn btn-success">Lưu</button>
        <a href="/" class="btn btn-danger">Hủy</a>
      </form>
    </div>
  </body>
</html>
